apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: default
type: Opaque
data:
  # The values must be base64 encoded. 
  # Example: echo -n "nats://nats:4222" | base64
  # Example: echo -n "redis" | base64
  NATS_URL: bmF0czovL25hdHM6NDIyMg==
  REDIS_HOST: cmVkaXM=
